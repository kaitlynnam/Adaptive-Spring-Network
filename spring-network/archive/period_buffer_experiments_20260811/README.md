# Superseded period-buffer experiments

These checkpoints and artifacts document ablations, tuning runs, and earlier
period-buffer models. They were moved out of the active result directories on
2026-08-11.

The active reference is:

`period_adaptive_3d_60spring_bounded_extended`

Active scripts default to that checkpoint and output prefix.
