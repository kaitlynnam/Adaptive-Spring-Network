# Active tables

`period_adaptive_3d/` contains deployment metrics, held-out summaries, and
many-profile results for the causal one-period-buffer controller. Tables from
profile-conditioned passive and other superseded pipelines are archived.
